<UserControl x:Class="HachBobAI.UI.SliderRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <Grid Margin="0,6,0,2">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="72"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Text="{Binding Label, ElementName=Root}"
                   Foreground="#AAAAAA" FontSize="12" VerticalAlignment="Center"/>

        <Slider x:Name="PART_Slider" Grid.Column="1"
                Minimum="0" Maximum="1"
                Foreground="#3A7BD5" Background="#1A3A60"
                VerticalAlignment="Center" Margin="8,0"
                ValueChanged="Slider_ValueChanged"/>

        <TextBox x:Name="PART_Entry" Grid.Column="2"
                 Background="#0D1A2A" Foreground="#4488FF"
                 BorderBrush="#2A4A70" BorderThickness="1"
                 FontFamily="Consolas" FontSize="11"
                 TextAlignment="Center" Height="26" Padding="2,3"
                 KeyDown="Entry_KeyDown" LostFocus="Entry_LostFocus"/>
    </Grid>
</UserControl>
